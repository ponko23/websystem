// Generated by CoffeeScript 1.6.3
var navWrite;

navWrite = function() {
  var navlist, obj, page, url, _i, _len;
  obj = [
    {
      "title": "Home",
      "path": "/"
    }, {
      "title": "Profile",
      "path": "/profile/"
    }, {
      "title": "TimeSheet",
      "path": "/timesheet/"
    }
  ];
  navlist = ['<ul>'];
  for (_i = 0, _len = obj.length; _i < _len; _i++) {
    page = obj[_i];
    navlist.push('<li><a href="', page.path, '">', page.title, '</a></li>');
  }
  navlist.push('</ul>');
  $('nav').append(navlist.join(''));
  url = location.pathname;
  return $("nav a[href=\"" + url + "\"]").css('background-color', '#fff');
};

/*
  $.ajax
    url: '/data/sitemap.json'
    dataType: 'json'
    type: 'GET'
    success: (obj) ->
      navlist = ['<ul>']
      for page in obj.siteMap
        navlist.push '<li><a href="', page.path, '">', page.title, '</a></li>'
      navlist.push '</ul>'
      $('nav').append navlist.join ''

      url = location.pathname
      $("nav a[href=\"#{url}\"]").css 'background-color', '#fff'
*/


$(document).ready(function() {
  return navWrite();
});
