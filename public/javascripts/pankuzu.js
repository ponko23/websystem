// Generated by CoffeeScript 1.6.3
var slideview;

$(function() {
  var aryUri, currentUrl, i, mono, myUrl, pankuzu, titleScr, titleStr, xhr, _i, _len;
  aryUri = location.pathname.split('/');
  currentUrl = aryUri.pop();
  if (currentUrl === "" || currentUrl === "index.html") {
    currentUrl = aryUri.pop();
  }
  if (currentUrl === "") {
    return;
  }
  myUrl = "http://" + location.host + "/";
  mono = ' > ';
  pankuzu = [];
  pankuzu.push(mono);
  aryUri.shift();
  for (_i = 0, _len = aryUri.length; _i < _len; _i++) {
    i = aryUri[_i];
    myUrl += "" + i + "/";
    xhr = new XMLHttpRequest();
    xhr.open('GET', myUrl, false);
    xhr.send(null);
    while (xhr.readyState !== 4 && xhr.status !== 200) {
      continue;
    }
    titleScr = /<title>(.*)<\/title>/;
    titleStr = titleScr.exec(xhr.responseText)[1];
    pankuzu.push("<a href=\"" + myUrl + "\">", titleStr, "</a>", mono);
  }
  pankuzu.push($('title').html());
  return $('#pankuzu').append(pankuzu.join(''));
});

slideview = function() {
  return $('#inputform').addclass('show');
};
